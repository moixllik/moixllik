# Testing en base a funcionalidades

> Pruebas a las features

En el archivo `test/main.feature` tenemos:

```
Feature:
  Scenario:
    Given:
    When:
    Then:
    And/But:
    Background:
    Examples:
```
